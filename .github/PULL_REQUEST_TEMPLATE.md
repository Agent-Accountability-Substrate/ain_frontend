## Summary

<!-- What does this PR change, and why? -->

## Checklist

- [ ] One concern per PR — this change does exactly one thing.
- [ ] CI is green (the `checks` job passes).
- [ ] Layering respected — the frontend only talks to `ain_backend_api`; no domain logic or storage added here.
- [ ] No secrets, key material, or PII in code, tests, fixtures, or logs.
- [ ] `pnpm-lock.yaml` regenerated by pnpm (never hand-edited), if dependencies changed.
